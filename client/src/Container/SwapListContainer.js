import React, { Component } from "react";
import SwapComponent from "../Component/SwapComponent";

class SwapListContainer extends Component {
  render() {
    return (
      <div>
        {/* {this.props.swaps.map((swap) => {
          <SwapComponent swap={swap} key={swap.id} />;
        })} */}
      </div>
    );
  }
}

export default SwapListContainer;
